// Frontend app version. Keep in sync with backend/config/version.py (APP_VERSION)
// and bump on every release. Used to cache-bust the fetched translation files so
// new/changed strings always load after an update instead of serving a stale copy.
export const APP_VERSION = '1.13.0';

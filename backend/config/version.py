"""Single source of truth for the application version.

Bump APP_VERSION on every release and tag the release on GitHub as ``v<APP_VERSION>``
(e.g. ``v1.13.0``) with release notes — the in-app update notification compares this
value against the latest GitHub Release.
"""

APP_VERSION = "1.12.1"

GITHUB_REPO = "aiulian25/soundwave"
GITHUB_REPO_URL = "https://github.com/aiulian25/soundwave"

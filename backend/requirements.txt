Django>=4.2,<5.0
djangorestframework>=3.14.0
django-cors-headers>=4.0.0
celery>=5.3.0
redis>=4.5.0
elasticsearch>=8.8.0
# Pinned deliberately (CNT-02). Bump via PR when YouTube extraction breaks; the CI
# trivy/grype gate flags known-vulnerable pins. Do not rely on runtime auto-update.
yt-dlp==2026.7.4
Pillow>=10.0.0
python-dateutil>=2.8.2
pytz>=2023.3
drf-spectacular>=0.26.0
django-celery-beat>=2.5.0
requests>=2.31.0
pyotp>=2.9.0
qrcode>=7.4.0
reportlab>=4.0.0
cryptography>=42.0.0
mutagen>=1.47.0
pylast>=5.2.0
psutil>=7.2.2
whitenoise>=6.5.0
gunicorn>=22.0.0
psycopg[binary]>=3.1.19
# F16 sonic analysis. Pulls numpy/scipy/soundfile/numba/llvmlite transitively and adds
# ~a few hundred MB to the image. Imported lazily in extract_features_task so non-feature
# code, the Celery worker, and CI tests all load without it. numpy pinned <2.0 for the
# numba/llvmlite ABI. Needs libsndfile1 (installed in the Dockerfile runtime stage).
librosa>=0.10.2
numpy>=1.26,<2.0
scipy>=1.11
